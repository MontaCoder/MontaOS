version: 0.2

phases:
    install:
        commands:
            - npm ci
    build:
        commands:
            - npm run typecheck
            - npm run build

artifacts:
    files:
        - '**/*'
